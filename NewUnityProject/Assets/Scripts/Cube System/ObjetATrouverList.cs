using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjetATrouverList : MonoBehaviour {
	/*
	GameObject Player;

	public GameObject[] Ville;

	bool FoundTown;

	public float distanceToTown;

	// Use this for initialization
	void Start () {
		Player = GameObject.Find ("Player");
		ZG = GameObject.Find ("ScriptManager").GetComponent<ZoneGestion> ();
	}
	
	// Update is called once per frame
	void Update () {
		if (ZG.AmIInsideArea) {
			distanceToTown = Vector3.Distance (Player.transform.position, Ville [0].transform.position);

			if (distanceToTown < 10) {
				print ("FOUND IT");
				FoundTown = true;
			}
			//outside area
			if (distanceToTown >= 10) {
				print ("STILL LOOKING"); 
				FoundTown = false;
			}
		}
	}*/
}
