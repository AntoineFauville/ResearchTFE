using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraRotationMoving : MonoBehaviour {
	/*
	float YCameraMain;
	float YPlayer;

	// Use this for initialization
	void Start () {
		YCameraMain = GameObject.Find ("Main Camera Main").GetComponent<Transform> ().rotation.y;
		YPlayer = GameObject.Find ("Player").GetComponent<Transform> ().rotation.y;
	}
	
	// Update is called once per frame
	void Update () {
		YCameraMain = GameObject.Find ("Main Camera Main").GetComponent<Transform> ().rotation.y;
		YPlayer = GameObject.Find ("Player").GetComponent<Transform> ().rotation.y;

		print (YCameraMain);
		print (YPlayer);

		if (Input.GetAxis ("Vertical") > 0) {
			YPlayer = YCameraMain;
		}
	}*/
}
